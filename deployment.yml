apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-deployment
  namespace: default
  labels:
    app: python
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python
  template:
    metadata:
      labels:
        app: python
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: python
        image: harshachess/flask-python
        ports:
        - containerPort: 5000
